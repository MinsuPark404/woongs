import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header>
            <h1>LOGO</h1>
            <button className="search-button">로그인(회원정보)</button>
        </header>
    );
};

export default Header;
